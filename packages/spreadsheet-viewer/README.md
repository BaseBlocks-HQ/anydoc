# AnyDoc Spreadsheet Viewer

A React spreadsheet viewer backed by the AnyDoc spreadsheet engine. Browser
reads run in a module worker when available, while the grid virtualizes rows and
columns and supports sheets, styles, charts, search, selection, and bounded copy.
