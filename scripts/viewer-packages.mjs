export const publicViewerPackageDirectories = Object.freeze([
  "packages/contracts",
  "packages/viewer",
]);

export const platformConsumerPackageDirectories = Object.freeze(publicViewerPackageDirectories);
