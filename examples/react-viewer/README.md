# React viewer example

Run `pnpm --dir examples/react-viewer dev`, then choose a local PDF, DOCX, XLSX, CSV, PPTX, Markdown, or text file. The example imports the umbrella package subpaths and keeps document bytes local to the browser.
